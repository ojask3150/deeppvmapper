# DeepPVMapper — training scaffold requirements (refs #11)
# The inference pipeline installs from requirements.txt (GDAL etc.); this file
# covers only train.py / eval.py and the BDAPPV experiments.

# Core
numpy>=1.22,<2
torch>=1.12
torchvision>=0.13

# Models: architectures (segmentation-models-pytorch) + timm encoders (tu-*)
segmentation-models-pytorch>=0.3.3,<0.5
timm>=0.9,<2

# Dataset snapshot + artifact upload (scripts/upload_results.sh)
huggingface_hub>=0.23

# Data handling / IO
pandas>=1.3
Pillow>=9.0
scipy>=1.7
pyyaml>=6.0
tqdm>=4.60
